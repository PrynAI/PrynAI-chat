# PrynAI Chat (monorepo)

- Chainlit UI → FastAPI Gateway → LangGraph Platform (Cloud).

- Docs: see /docs. Architecture: single region, Key Vault via Managed Identity, ACR/ACA, Front Door/WAF (later).